syntax = "proto3";

import "common.proto";

service SchedulerToWorker {
    // Starts a job on worker.
    rpc Run (RunRequest) returns (Empty) {}
}

message RunRequest {
    uint64 job_id = 1;
    string command = 2;
    uint64 num_epochs = 3;
}
