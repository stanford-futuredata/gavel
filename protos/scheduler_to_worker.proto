syntax = "proto3";

import "enums.proto";

service SchedulerToWorker {
  // Starts a job on a worker
  rpc Run (RunRequest) returns (RunResponse) {}
}

message RunRequest {
  uint64 job_id = 1;
  string command = 2;
}

message RunResponse {
  uint64 job_id = 1;
  JobStatus status = 2;
}
